import React from 'react';
import { useForm } from 'react-hook-form';

export interface BracketPoolRangeEditorProps {

}

const BracketPoolRangeEditor = ({}: BracketPoolRangeEditorProps) => {
    // TODO: Text field of to/from range
    return <div />;
};

export default BracketPoolRangeEditor;
